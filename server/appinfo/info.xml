<?xml version="1.0"?>
<info xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://apps.nextcloud.com/schema/apps/info.xsd">
    <id>crispcloud_delta</id>
    <name>CrispCloud Delta Sync</name>
    <summary>Block-level delta sync for CrispCloud file manager</summary>
    <description><![CDATA[
Companion server app for CrispCloud that enables block-level delta sync.
Compatible with both Nextcloud and ownCloud 10.

Instead of re-uploading entire files when only a few blocks changed, this app
maintains block-level indexes (Adler-32 + SHA-256 hashes per 4 MB block) and
accepts partial block updates via a REST API.

Ideal for large files that change incrementally: VeraCrypt containers, database
files, disk images, VM snapshots, PST archives.

## API Endpoints

- `GET  /api/blockmap/{path}` — retrieve the block map for a file
- `POST /api/blocks/{path}?offset=N&size=M` — write a single block at offset
- `POST /api/finalize/{path}?size=N` — finalize after block writes (truncates to N bytes if smaller)
- `GET  /api/status` — check if the app is installed and working

## Compatibility

- Nextcloud 25–33
- ownCloud 10.11+ (uses shared OCP\Files and OCP\AppFramework APIs)
- ownCloud Infinite Scale (oCIS): use the Go sidecar in ocis/ instead
    ]]></description>
    <version>0.3.0</version>
    <licence>agpl</licence>
    <author>CrispCloud Contributors</author>
    <namespace>CrispCloudDelta</namespace>
    <category>files</category>
    <dependencies>
        <!-- Works on both Nextcloud 25+ and ownCloud 10.11+ (PHP 7.4+) -->
        <!-- ownCloud 10 uses PHP 7.4; Nextcloud 25+ uses PHP 8.0+        -->
        <php min-version="7.4"/>
    </dependencies>
</info>
